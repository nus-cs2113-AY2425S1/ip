package chattycharlie.commands;

/**
 * Represents the different types of commands that can be executed by ChattyCharlie.
 * Each enum value corresponds to a specific command type.
 */
public enum CommandType {
    TODO,
    DEADLINE,
    EVENT,
    MARK,
    UNMARK,
    BYE,
    LIST,
    DELETE,
    PRINT,
    FIND
}